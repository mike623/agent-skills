repos:
  - repo: local
    hooks:
      - id: agent-skills-public-safety
        name: Agent skills public-safety scan
        entry: python3 scripts/security_scan.py --staged
        language: system
        pass_filenames: false
        stages: [pre-commit]
